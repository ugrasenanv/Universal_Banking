/**
 * Identity Service module.
 *
 * Provides federated authentication (OIDC, SAML, FAPI 2.0),
 * RBAC + ABAC access policy enforcement, session management,
 * and Strong Customer Authentication.
 */
export { IdentityService } from './identity-service.js';
export type { IdentityServiceConfig } from './identity-service.js';
export type { AuthProtocol, SCAMethod, SessionConfig, AuthenticationFlow, AuthenticationRequest, AuthenticationResult, AuthenticationError, AuthCredentials, Session, Role, Permission, AttributeCondition, SegregationRule, AccessPolicy, AccessRequest, AccessDecision, } from './types.js';
//# sourceMappingURL=index.d.ts.map